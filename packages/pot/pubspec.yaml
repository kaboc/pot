name: pot
description: An easy and safe DI (Dependency Injection) solution for Dart with support for scoping.
version: 0.6.0
repository: https://github.com/kaboc/pot

topics:
  - dependency-injection

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  meta: ^1.8.0

dev_dependencies:
  lints: ^2.0.1
  test: ^1.24.3

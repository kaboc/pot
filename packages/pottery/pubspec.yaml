name: pottery
description: Widgets to limit the scope where particular Pots are available in the widget tree.
version: 0.1.1
repository: https://github.com/kaboc/pot

topics:
  - dependency-injection

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

  pot: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
